# DEV-24 – VaultSweeper (DAO reclaim + whitelist + guards + tests)

**Scope**
- Adds VaultSweeper DAO module
- Allows DAO to reclaim non-collateral tokens
- Collateral whitelist with protection guard
- NonReentrant + Pausable enforced

**Tests**
✅ DAO-only access  
✅ Zero-amount revert  
✅ Whitelisted token protection  
✅ Event emissions for sweep + whitelist  

**Release:** v0.30 — VaultSweeper added (DAO maintenance module)
