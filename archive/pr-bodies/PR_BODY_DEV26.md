# DEV-26 – OracleAdapter Minimal (v0.32)

**Scope**
- Minimal on-chain oracle adapter for PSM
- DAO-only `setPrice()` with event emission
- `getPrice()` reverts on stale data (>24h)
- Tests: DAO guard, event, stale revert

**Release:** v0.32 — OracleAdapter base module ready
