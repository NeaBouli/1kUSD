== DEV-38 DIAG: swapTo1kUSD revert-Analyse ==
UTC: 2025-11-04T20:30:18Z

[1/3] Funktionsdefinitionen rund um swapTo1kUSD:
    function swapTo1kUSD(
        address tokenIn,
        uint256 amountIn,
        address to,
        uint256 minOut,
        uint256 /*deadline*/
    )
        external
        override
        whenNotPaused
        nonReentrant
        returns (uint256 netOut)
    {
        IERC20(tokenIn).safeTransferFrom(msg.sender, address(vault), amountIn);
        uint256 fee = (amountIn * mintFeeBps) / 10_000;
        netOut = amountIn - fee;
        if (netOut < minOut) revert InsufficientOut();
        oneKUSD.mint(to, netOut);
        emit SwapTo1kUSD(msg.sender, tokenIn, amountIn, fee, netOut, block.timestamp);
        return netOut;
    }

[2/3] Alle require/assert/revert-Zeilen:
35:            revert PausedError();
69:        if (netOut < minOut) revert InsufficientOut();
85:        if (netOut < minOut) revert InsufficientOut();

[3/3] SafeERC20-Aufrufe (potenzielle Revertquellen):
5:import {SafeERC20} from "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";
16:    using SafeERC20 for IERC20;

Log gespeichert unter: logs/dev38_diag_psm_swapfail_20251104T203018Z.log
