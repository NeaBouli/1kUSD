--- a/.github/workflows/forge-ci.yml
+++ b/.github/workflows/forge-ci.yml
@@
       - name: Ensure OpenZeppelin is available
         run: |
           echo "📦 Installing OpenZeppelin Contracts..."
           if [ ! -d "lib/openzeppelin-contracts" ]; then
             forge install OpenZeppelin/openzeppelin-contracts --no-commit
           fi
           echo "✅ OpenZeppelin Contracts installed."
+
+      - name: Ensure remappings.txt exists
+        run: |
+          echo "🔧 Generating remappings.txt for CI..."
+          echo '@openzeppelin/=lib/openzeppelin-contracts/' > remappings.txt
+          cat remappings.txt
+
+      - name: Verify OpenZeppelin presence
+        run: |
+          test -d lib/openzeppelin-contracts && echo "✅ OZ directory OK" || (echo "❌ OZ missing" && exit 1)
